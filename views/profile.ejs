<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet"/>
</head>
<body class="bg-zinc-900">
    <div class="w-full  bg-zinc-900 text-white p-10">

        <div id="nav" class="nav fixed top-0 -left-[30%] text-black h-screen w-[30%] flex flex-col gap-7 py-44 px-12">
            <div class="h-7 w-7 rounded-full  text-black flex items-center absolute top-8 left-9 justify-center font-medium text-3xl">
                <i class="close ri-close-fill cursor-pointer"></i>
            </div>
            <a class="text-3xl font-medium" href="/profile"><i class="ri-profile-line mr-2"></i>Profile</a>
            <a class="text-3xl font-medium" href="/newpost"><i class="ri-image-add-fill mr-2"></i>New Post</a>
            <a class="text-3xl font-medium" href="/profile/upload"><i class="ri-image-add-fill mr-2"></i>Update Profile Picture</a>
        </div>

        <div class="w-full flex justify-between ">
            <i class="menu ri-menu-3-line text-2xl cursor-pointer"></i>
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <div class="flex gap-3  justify-center overflow-hidden">
            <img class="h-14 w-14 rounded-full bg-red-500 object-cover" src="/images/uploads/<%= user.profilepic%>" alt="">
            <h3 class="text-3xl mt-2  mb-6"><span class="font-light">Hello,</span> <%= user.name%>👋</h3>
        </div>
            <h3 class="mt-10 text-xl font-medium">Your Posts</h3>
            <% if(user.posts.length > 0) { %>
                <div class="posts mt-10 flex gap-2 flex-wrap">
                    <% user.posts.forEach(function(post){ %>
                        <div class="post mt-5 w-1/4 p-5 bg-zinc-700 rounded-md border-[1px] border-zinc-700">
                            <h2 class="text-blue-500 mb-3 font-medium"><%= user.username %></h2>
                            <div class=" mt-5 mb-5 h-36 w-full overflow-hidden  rounded-md bg-blue-400">
                                    <!-- <% if(post.postImage.length > 0 ){ %> -->
                                        <img class="h-full w-full object-cover" src="/images/uploads/<%= post.postImage %>" alt="">
                                    <!-- <% } else { %>
                                        <video src="/videos/uploads/<%= videoUrl %> " controls muted autoplay ></video>
                                    <% } %> -->
                            </div>
                            <p class="text-md tracking-tight"><%= post.content%></p>
                            <small class= "mt-3 inline-block"><%= post.likes.length%> likes</small>
                            <div class="btns flex gap-3 mt-2">
                                <a class="text-blue-500 " href="/like/<%= post._id %>">
                                    <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                                </a>
                                <a class="text-red-500" href="/delete/<%= post._id%>">Delete</a>
                            </div>
                        </div>
                    <%})%>
                </div>
            <%} else { %>
                <h3 class="mt-14 text-zinc-400">No Posts Yet.</h3>
            <% }%>
           
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js" integrity="sha512-7eHRwcbYkK4d9g/6tD/mhkf++eoTHwpNM9woBxtPUBWm67zeAfFC+HrdoE2GanKeocly/VxeLvIqwvCdk7qScg==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <script src="/javascripts/script.js"></script>
</body>
</html>